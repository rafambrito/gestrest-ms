spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/gestrest
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update    
    defer-datasource-initialization: true

  sql:
    init:
      mode: always